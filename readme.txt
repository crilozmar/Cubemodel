You will also need to install:
- Arduino
- Teensyduino (find it in tensy32_stuff)
	- This one will ask you for the root of arduino installation, which by default should be in: 
	
- If after doing everything it does not work, change the USB cable (even if you see some led turning on when plugging it, the cable can still be failing)